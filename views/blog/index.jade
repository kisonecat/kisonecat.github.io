extends ../layouts/main

block content
    mixin breadcrumbs(['/blog'],['Blog'])

    - var posts = getPosts(0,12)
    - var page  = 1
    - each post in posts
      include includes/postSnippet

    include includes/pagination
